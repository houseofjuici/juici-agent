{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/agent/(?<id>[^/]+)$", "dest": "/agent/[id]?id=$id" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
} 